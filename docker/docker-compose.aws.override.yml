# AWS 배포 시 사용하는 오버라이드 (비밀값 주입)
# 사용: docker-compose -f docker-compose.aws.yml -f docker-compose.aws.override.yml up -d
# 아래 비밀번호는 난수로 생성된 값이며, 필요 시 변경하여 사용하세요.

services:
  postgres:
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "Lg8pQ2mN5vR9xW4kY7zA1bC3dE6fH0j"

  lotto-api:
    environment:
      - DB_USERNAME=postgres
      - DB_PASSWORD=Lg8pQ2mN5vR9xW4kY7zA1bC3dE6fH0j
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=Lg8pQ2mN5vR9xW4kY7zA1bC3dE6fH0j
